# Local compiler and LLVM checkouts. These are real Git repositories/worktrees
# managed separately from this coordination repo.
/main/oxcaml/
/main/llvm-project/

# Agent directories are generated locally by scripts/use-or-create-agent.
# Canonical per-agent state lives in each OxCaml branch under agent-state/.
/agents/*
!/agents/.gitkeep

# Build and editor noise.
.DS_Store
*.swp
*~
